Next steps:

-1: Check last commit and do the same for updates_roots (split to roots and leaves); you need to traverse the graph of all packages and extract the roots and leaves from there rather than the update-able packages.

0. Update CLAUDE.md and README.md.

1. Let's add to depgraph.json groups and "last updated" based on the Groups and Install Date data from pacman -Qi. Update applet.json and the Rust code to read/write these two. Add a "last updated" column in the Rust app

2. Refactor: create a separate Rust binary that rebuilds the depgraph.json and extract the shared logic in a Rust library; then: applet.js should call Rust to rebuild depgraph.json and not do it itself.

3. Let's have deps/used-by always shown in the first column. Highlight deps if in deps mode; used-by if in used-by mode.
